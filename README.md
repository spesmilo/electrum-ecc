This packages provides a pure python interface to libsecp256k1.

Unlike Coincurve, it uses ctypes, and has no dependency.

### Maintainer notes

To build sdist for PyPI:
```
$ ./contrib/release.sh
```
